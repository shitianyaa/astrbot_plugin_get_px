# 更新日志

## v3.8.0 (2026-09-12)

### 新增
- 会话策略新增独立自定义屏蔽词和作品 ID 黑名单，并支持按字段批量应用到现有群聊/私聊策略。
- 新增统一“会话策略配置”界面：AstrBot 插件配置页和管理中心均可持久化添加、编辑、删除群/私聊策略；删除后恢复默认严格策略，升级时一次性导入旧 SQLite 策略。
- 会话策略迁移兼容旧 schema v3：配置保存成功后备份并将数据库版本收敛回 v2，永久保留 SQLite 群策略表与历史行；迁移失败保留 v3 数据。

### 变更
- 内置安全词开启时使用内置词与全局列表，关闭时仅使用当前会话独立列表。
- 管理 API 保留既有内容安全字段，并新增群策略查询和原子更新端点。

## v3.7.0 (2026-09-06)

### 新增
- 签到商店上架"万象画卷生图额度"商品：按张定价，用户可自选购买张数（`签到商店 生图 <张数>`，范围 1–50）；单价由 `checkin_omnidraw_quota_cost` 配置（默认 75，范围 0–300，0 为免费），实际花费 = 单价 × 张数。商品仅在同时安装万象画卷且其启用每日生图限制时展示，发放失败自动退回金币。**管理员应当根据群成员的平均金币数量调整单价**——每日签到基础奖励为 50–100 金币（连签最多 +50），默认 75/张约为一天签到收入买一张图。
- 签到联动：同时安装万象画卷时，本插件 `/签到` 在发放金币与签到卡的同时，自动为万象画卷当日生图额度增加随机 1–3 张（读取对方 `checkin_bonus_min/max` 配置），与对方 `/签到` 双向幂等（通过 `checkin_at` 标记，谁先跑都只发一次）。本插件的 `/签到` 通过 `stop_event` 自动屏蔽万象画卷的同名指令，无需手动禁用。
- 额度过期机制：生图额度（含签到赠送与商店购买）均为**当日有效**，万象画卷每日跨天时自动清零（bonus 与 count 一起重置），不结转到次日。
- 插件启动时检测万象画卷安装、版本、每日生图限制与签到开启状态，并在日志输出共存提示（版本过旧、未启用每日限制、签到联动已激活等情形）。
- `/签到状态` 新增万象画卷生图额度展示（剩余/已用/加成），桥不可用时不显示。
- 签到商店结算底层新增通用 `add_coins` 金币入账方法，用于购买发放失败时的原路退款。

### 变更
- 配置 schema 从扁平结构改为 6 个 object 分组（Pixiv 图源、图片去重、万象联动、签到基础、签到商店、运行参数）。WebUI 配置页按分组折叠展示。`main.py` 配置读取层先遍历分组取值、找不到再回退扁平 key。
- schema 顶层保留 `invisible` 旧扁平键作为过渡兼容。AstrBot 4.27+ 加载插件配置时会删除 schema 之外的键，这些 invisible 键让旧扁平值在框架裁剪前存活，`_migrate_grouped_config` 随后搬到对应分组。迁移完成后下一版本将删除这些顶层键。
- 收紧 5 项定价/阈值配置的 slider 上限，代码侧读取范围同步对齐：`p_coin_cost` 500→200、`auto_downgrade_original_mb` 100→25、`checkin_omnidraw_quota_cost` 1000→300、`checkin_omnidraw_quota_daily_max` 100→30、`checkin_background_refresh_cost` 500→300。
- 签到备份快照新增 `omnidraw_quota_purchases` 表导出/导入，每日购买计数不再因恢复而丢失。旧快照缺该字段时兼容导入。

## v3.6.1 (2026-09-04)

### 修复
- Pages 管理中心群列表在 QQ 官方 Bot 32 位 OpenID 群号下溢出卡片右边界：补全 `min-width:0` 弹性约束与文本截断，长群号不再撑破布局。

### 新增
- Pages 成员数值弹窗增加好感度加持状态展示与调整：列表显示加持徽章（生效中·余 N 天），弹窗支持保持/清除/延长 1·3·7 天，日期计算与用户购买路径一致（当天已签到则从次日起算）。
- Pages 数据管理增加缓存占用统计与一键清理：展示签到卡片缓存与临时下载文件的磁盘占用，一键安全清理不影响签到数据。
- 签到帮助图补全加持/查看商品/生日/称号四条 `.note` 说明，图片版本升至 v4.1。

## v3.6.0 (2026-08-29)

### 新增
- `/p` 与自然语言触发发图接入金币计费：新增 `p_coin_cost` 配置（默认 20，设为 0 免费），按实际发送张数扣费；发送成功后提醒消耗张数与余额，余额不足仅提示、不扣费不发图（底层新增通用 `spend_coins` 钱包方法与结算回归测试）。
- 新增个人月度签到日历：`签到日历 [YYYY-MM]` 以 T2I 图片展示当月签到状态、金币与节气/节日（lunar_python 节气与农历节日、法定节假日、管理员自定义事件），默认当月、仅可查看当前及过去月份，按天缓存复用；输出分辨率与背景画质完全对齐签到卡三档（省流量=1600×900+medium 背景、清晰=2080×1170+large、极致=2880×1620+large）。
- `/签到帮助` 帮助图改为自包含 T2I 模板重制（`templates/checkin_help/`，内联 CSS 与 LXGW 字体），指令清单同步新指令面。

### 修复
- 修复 `/p` 金币结算在扣费提交后回读档案：回读失败不再把已扣费请求误报为“本次不扣费”，成功结果改为事务内已算好的余额直接返回。
- 签到日历缓存 key 纳入当月签到记录摘要：当日签到后重看日历立即重渲，不再显示“未签到”与旧的金币累计。
- 签到日历全局事件读取失败时降级为空事件列表继续出图，不再因读库异常导致整个月度日历图“生成失败”。
- 签到日历背景图 base64 编码移入线程池执行，原图较大时不再阻塞事件循环。
- 签到背景的 Lolicon 请求改用合法的 `aspectRatio` 数值条件语法（按 3:4±容差推导 `gt0.60lt0.90`）：旧 `"vertical"` 枚举不是合法参数、端上等于未筛；现在端上先筛再本地复核，减少无效候选下载。
- 修复帮助图固定画布裁切：「个人-称号」「排行-累计」「签到管理 事件删除」等溢出条目不再缺失；画布改为宽度 1440 固定、高度随指令内容自适应（full_page 截图，不再传 clip），后续新增指令无需再调尺寸。
- 签到日历缓存 key 纳入自定义事件指纹：管理员当日增删自定义事件后同月日历立即重渲，不再命中当日旧图；日历事件名输出统一 HTML 转义，特殊字符不再破坏模板渲染。

### 变更
- 签到指令体系重组（方案 C，破坏性）：`签到状态`、`签到成就` 拍平为独立指令；生日收为 `签到生日 查看|设置|清除`；称号收为 `签到称号 查看|佩戴`；主题从商店迁出为 `签到主题 列表|查看|购买|切换`；`刷新背景` 抽成独立顶层指令。旧 `签到我的` 组与 `签到商店 主题*`、`签到商店 刷新背景` 写法不再注册，无别名过渡。

### 文档
- 新增 `docs/dev/t2i-rendering.md`：T2I 渲染链路、各资产画布约定与自建端点本地重渲指南。

## v3.5.1 (2026-08-14)

### 变更
- `send_as_forward` 已由 `forward_threshold` 整数阈值取代：仅 aiocqhttp 平台可使用合并转发，`0` 始终合并转发，`1` 表示成功下载图片超过 1 张时合并转发。旧布尔值仅在新字段缺失时兼容，`true` 等价于 `0`，`false` 等价于最大单次发图数。

## v3.5.0 (2026-08-01)

### 新增
- 新增新柳、荷风、丹枫、寒梅四套签到卡主题，对应主题编号 `04`–`07`，支持主题商店预览、购买和切换。
- 新增四季主题的自包含模板、预览图和开发期装饰构建脚本；签到卡主题展示统一使用竖向 `3:4` 画框。

### 优化
- 统一四季主题的卡片边距、信息栏比例、插画框尺寸和标题溢出处理，减少主题装饰越界与文字截断。
- 清理四季主题画框附加文案、季节字框和问候区冗余圆形装饰，编号改为标题前的小号无框标识。

### 文档
- README 的签到卡主题示意图更新为春、夏、秋、冬四套预览，并同步主题编号说明。
- 补充四季主题模板结构、预览构建流程和本地示意图渲染说明。

## v3.4.1 (2026-07-22)

### 优化
- 整理高频日志级别和字段格式：正常限流、自动回退、重试及可选能力超时不再作为故障告警；搜索标签和完整用户 ID 不再写入日志。

### 变更
- 签到生日、称号、主题与管理事件指令扁平化：`/签到我的 生日查看|生日设置|生日清除`、`/签到我的 称号查看|称号佩戴`、`/签到商店 主题列表|主题查看|主题购买|主题切换`、`/签到管理 事件查看|事件添加|事件删除`；旧三级写法不再注册。

### 升级说明
- 原 `/签到我的 生日 ...`、`/签到我的 称号 ...`、`/签到商店 主题 ...` 和 `/签到管理 事件 ...` 三级指令需改用上述新指令；可通过 `/签到帮助` 查看完整写法。

## v3.4.0 (2026-07-20)

### 修复
- 修复图片下载器 `download()` 方法使用 `os.path.getsize()` 可能存在的文件系统缓存延迟问题，改为在下载时实时统计字节数并返回 `(path, size)` 元组。
- 修复 `download()` 方法类型注解不匹配问题，从 `-> str` 更正为 `-> tuple[str, int]`。
- 修复 `dedupe_ttl_hours` 数值仅被当作开关、名称与实际行为不一致的问题；新增最近 `0–7` 个北京时间自然日去重，并在缩短窗口时清理超期索引。
- 修复 AI 问候错误角色或单标记 Markdown 可能被保存、远程问候升级写入失败会中断签到卡、管理员预览问候异常会直接中断的问题。
- 修复去重清理失败后仍被限频、配置窗口未回滚，以及图片下载部分临时文件和文件描述符异常路径未释放的问题。
- 修复关闭去重时签到发送失败会错误撤销背景、昵称缺失时用户 ID 进入 AI 问候上下文，以及无缓存背景刷新和画质探测残留 T2I 临时文件的问题。
- 修复修改签到卡画质后主动刷新背景仍复用当天旧档位的问题；恢复普通作品最终下载和逐条发送成功的 `info` 日志。

### 优化
- 优化 AI 问候数据传递：移除值为"无"的空字段，精简提示词并明确说明只使用存在的信息。
- 提取 `_prepare_configurable_instruction` 函数专门处理 AI 问候提示词模板清理，提升可读性。
- 提取 `_plan_download_attempts` 函数处理图片下载路由预算分配，降低嵌套深度。

### 新增
- 新增 `省流量`、`清晰`、`极致` 三档签到卡画质，渲染失败时按档位降级并使用精确尺寸缓存校验。
- 新增多个 Lolicon 图片反代 origin 的顺序轮换、原地址兜底和质量降级；不影响 Pixiv 登录直连策略。

### 变更
- 移除独立 `checkin_card_quality` JPEG 质量配置，签到卡统一使用 JPEG 质量 95。
- 旧 `dedupe_ttl_hours` 从配置界面隐藏，并一次性迁移为新的 `dedupe_days`。
- 去重、签到渲染缓存、图片反代、AI 问候和一言统一增加脱敏结构化结果日志；普通作品最终下载和发送成功使用 `info`，其他高频成功与格式拒绝使用 `debug`，故障告警按原因限频，不记录提示词、回复正文、完整 URL 或用户标识。
- 签到命令改为 `签到我的`、`签到排行`、`签到商店`、`签到管理` 四个独立指令组，移除旧 `签到中心` 路由，并重新生成 `/签到帮助` 帮助图。

### 升级说明
- **AI 问候优化**：系统现在只会传入有效信息，不再传入"无"的空字段。如果你使用了自定义 AI 问候提示词（`checkin_ai_greeting_prompt`），建议在提示词末尾添加："根据提供的数据生成问候语，只使用存在的信息，缺失的信息不必提及。"

## v3.3.2 (2026-07-18)

### 修复
- 修复 `/p [标签] [数量]` 只绑定第一个参数、导致尾部数量丢失并始终只发送一张图片的问题；同时保留 `/p` 无参数调用兼容性。

### 测试
- 新增 `/p` 贪婪参数注册、尾部数量保留及标签与数量拆分回归测试。

## v3.3.1 (2026-07-18)

### 变更
- 默认分支移除 `pixiv_proxy_url` 配置以及 Pixiv 登录、图片下载链路中的代理参数，统一使用本机直连。

### 文档
- README 明确需要代理时使用仓库的 `proxy` 分支。

## v3.3.0 (2026-07-17)

### 修复
- 整数配置不再将布尔值或浮点值静默转换、截断为整数，非法值统一回退到配置默认值。

### 变更
- 新增 `checkin_theme_cost` 配置，可在 0–5000 金币范围内统一设置非默认签到主题价格；默认主题始终免费。

### 文档
- README 展示插件 Logo、补充 Logo 来源致谢，并新增浏览计数器。

## v3.2.0 (2026-07-15)

### 修复
- 新增独立 `/签到帮助` 发送重制后的大字号帮助图，明确完整指令与必填参数；`/签到中心` 只保留为唯一指令组，避免同名普通指令被 AstrBot 指令管理判定冲突。
- 生日和管理事件现在接受帮助图中展示的“查看”动作，同时兼容原有省略动作及事件“列表”写法。
- `/pd <作品ID> [页码]` 的页码此前不会传入插件（指令解析按声明形参绑定并丢弃多余输入），多页作品始终发送第 1 页；现在页码作为独立参数正常生效。
- `/p` 支持包含空格的多词标签（如 `/p sword art online 3`），末尾数字视为数量。

### 变更
- 移除 `/pr`、`/prl`、`/pi`、`/pd` 指令和 Pixiv 排行榜逻辑；Lolicon 改为首选图片源，失败时有标签回退 Pixiv 搜索、无标签回退 Pixiv 推荐作品。
- 签到商店拆分为独立模块，商品目录使用稳定 ID 和分类集中注册，购买、主题与付费背景刷新逻辑不再堆积在通用签到命令模块中。
- 将签到功能收拢到 `/签到中心` 的“我的、排行、商店、管理”四个分组，删除旧平铺业务指令；`/签到`、`/签到帮助` 与纯文本“签到”保持独立入口。
- 签到商店和参数错误中的运行时指令提示改为不带前缀的命令路径，实际唤醒前缀统一交由 AstrBot 处理。
- 移除 `/ph` 帮助指令；指令说明见 README 与当前指令文档。
- 版本号统一带 `v` 前缀；移除 metadata 中无效的 `dependencies` 字段（依赖以 `requirements.txt` 为准）。
- 新增 MIT LICENSE。

### 改进
- 插件卸载流程可重入并增加终止标志；初始化中的 SQLite 建库与缓存清理移入线程池，不再阻塞事件循环。
- 签到卡片视图模型构建（含背景图 Data URL 编码）与缓存完整性校验移入线程池。
- AI 问候与一言请求失败时输出日志便于排查；一言请求复用 HTTP 会话并在卸载时关闭。
- Pixiv 搜索与推荐作品请求失败自动重试一次，缓解瞬时网络抖动。

## v3.1.0 (2026-07-15)

### 改进
- 完成签到数据升级收尾，移除 v2.8.x 旧表迁移、迁移备份和临时 SQLite 数据库替换入口。
- 签到存储只接受当前数据库结构；检测到未升级的旧版数据库时会拒绝加载，并提示先使用 3.0.0 完成迁移。
- 管理中心恢复为仅导入 schema v6 JSON 备份，不再接受 `.sqlite3` 或 `.db` 文件。

## v3.0.0 (2026-07-14)

### 改进
- 以全新市场插件为基线，主题采用固定内部 ID 与 `00`–`03` 编号，并拆分用户、主题和用户主题关系表。
- 从 v2.8.x 更新时首次启动会自动备份并迁移旧签到用户、偏好和主题购买数据；迁移校验成功后删除旧表，已经启动过早期 v3 的新增收益也会合并保留。
- 签到备份升级为 schema v6，仅接受当前结构，避免静默导入不兼容数据。
- 管理中心可临时导入新版 SQLite 签到数据库；校验通过后原子替换并删除旧数据库及其 WAL/SHM 文件，不保留旧数据库副本。
- 安装或更新后会记录插件版本及签到数据库创建/加载状态；导入备份、覆盖旧数据和删除旧数据库时输出明确的操作结果日志。
- 移除旧图片历史缓存清理、旧配置迁移、旧模块别名和数据库字段补丁逻辑。
- 精简发布资源，移除手工联网脚本、主题预览生成脚本及其样例素材。

## v2.8.0 (2026-07-13)

### 新增
- 新增 `/查看主题 <编号>`，所有用户都可免费查看 `00` / `01` / `02` / `03` 内置预览图，不会扣金币或修改签到数据。
- 新增 `/签到主题`、`/购买主题 <编号>` 和 `/切换主题 <编号>`，统一管理签到主题购买与切换。
- 新增 `/刷新签到背景`，花费金币重新抽取当天 Pixiv 背景；一言模式下会同时刷新问候并更新当天卡片，失败时保留原问候。
- 新增 `/签到排行 [今日|月榜|连签|累计]`，按群独立统计签到排行，并提供近 7/30 天趋势。
- 新增按群隔离的今日、月度、连签和累计签到排行，并提供近 7/30 天趋势。
- 签到备份升级为 schema v5，完整保存和恢复群签到 presence。
- 新增不可关闭的内置内容安全词库、自定义安全词管理和手动作品 ID 黑名单。
- WebUI 重构为「插件管理中心」，集中提供群排行、内容安全和数据管理。
- 管理中心新增签到成员搜索与数值编辑，可调整金币、好感度、累计签到和连续签到，不改写历史签到记录。

### 安全
- 所有搜索、排行榜、作品详情、ID 下载和签到背景固定只接受 Pixiv 普通分级作品。
- 用户搜索词在请求前检查；作品返回后继续检查标题、简介、标签和翻译标签。
- 安全词或作品黑名单读取异常时拒绝发送对应作品。

### 改进
- 已上线签到主题使用固定编号和颜色名称：`00` 米白、`01` 浅蓝、`02` 红黑、`03` 黄黑。
- 完全移除发图历史及其缩略图记录和 Web API。
- 移除发图流程中的 AI 识图评论功能及其配置项，减少发送链路和模型依赖。
- 插件每次初始化都会安全清理旧 `image_history` 与签到卡片缓存，并输出文件数、字节数和结果日志。
- 作品黑名单增加原因、操作者和加入时间字段。
- 整理发布文件：移除旧版签到截图和可再生成的主题预览 HTML，将签到事件矩阵生成器归入 `scripts/`。

## v2.7.0 (2026-07-12)

### 新增
- 签到卡片升级为固定 `960 × 540` 的“H · 丰富信息签到卡片”，左侧展示完整签到信息，右侧使用固定竖向作品相框。
- 新增本地文案、一言和 AI 三种签到问候来源；默认使用一言，远程请求失败时保留本地文案。
- 一言请求和本地响应校验统一限制正文最多 24 个字符。
- 一言类型支持在配置页使用中文多选，每次只在所选分类范围内随机返回一句。
- 新增一天期签到卡片 JPEG 缓存；同一天重复签到优先重发同一张卡片，缓存缺失或损坏时根据当天记录重建。

### 改进
- Pixiv 签到背景固定使用 `medium` 画质，避免先下载原图再降级；普通发图画质配置不受影响。
- Pixiv 签到作品固定按 `3:4 ± 20%` 筛选，只接受宽高比 `0.60–0.90`；无匹配作品时使用内置纸张占位图，不再放宽到横图或方图。
- 作品以 `object-fit: contain` 完整显示，原始图片仅以 Data URL 只读注入，不裁切、不缩放写回。
- 重复签到不再发放奖励，也不再扣除好感度；已保存的问候和作品保持不变，且不会再次调用模型。

### 配置
- 新增 `checkin_greeting_mode`、`checkin_hitokoto_categories`、`checkin_hitokoto_timeout`、`checkin_ai_greeting_provider_id`、`checkin_ai_greeting_prompt`、`checkin_ai_greeting_timeout`。
- 移除已不再生效的 `checkin_background_aspect_ratio` 与 `checkin_background_aspect_tolerance` 设置项；签到背景比例继续由 V2 固定契约控制。

## v2.6.1 (2026-06-04)

### 改进
- 签到 Pixiv 背景多个标签改为每次签到随机确定尝试顺序；一个标签无可用候选时继续尝试下一个。
- 签到 Pixiv 背景选图同时参考当天 SQLite 索引和图片历史，优先避开已发送作品。
- 签到 Pixiv 背景当前页候选全都用过时会立即推进分页并拉取下一页。
- 正式签到下载 Pixiv 背景前会写入 `checkin_pending` 预占用，下载、渲染或发送失败时释放占用，减少并发签到拿到同一张背景的概率。

### 修复
- 修复自定义签到背景不可用时直接 fallback 默认背景的问题；现在会继续尝试 Pixiv 背景，Pixiv 也不可用时才 fallback。
- 修复签到背景和黑名单标签配置使用中文标点或换行分隔时未正确拆分的问题。
- 修复签到背景多标签只尝试当天选中的单个标签，失败后不继续尝试其它配置标签的问题。

### 测试
- 新增签到背景选择回归测试，覆盖自定义背景回退 Pixiv、索引与历史共同去重翻页、预占用释放和标签搜索。

## v2.6.0 (2026-06-03)

### 新增
- 新增签到自定义背景完整性校验，损坏或非图片文件会被跳过。
- 新增下载大小上限保护，超出 50 MiB 的图片会被拒绝。

### 改进
- 图片下载改为流式写入临时文件，避免大图响应整体进入内存。
- 原图超过自动降级阈值时继续清理旧临时文件，降低多图发送时的资源压力。
- 图片历史 JSON 损坏时会备份为 `.corrupt.<timestamp>` 文件，再以新历史继续写入，避免静默覆盖损坏原件。
- Pixiv 作品详情只在明确缺失时返回“未找到”，网络、登录或 API 异常会向命令层传递并记录日志。
- WebUI 图片历史和黑名单 API 的内部错误响应改为通用文案，详细异常仅写入日志。
- 已核验 AstrBot 插件页面文档：Bridge 会转发到 `/api/plug/<plugin_name>/...`，文档只要求插件名前缀，未发现插件侧额外鉴权 API；本版不新增自定义鉴权层。

### 修复
- 修正配置提示中的旧排行榜命令为 `/prl`。
- 修正 `/ph` 帮助中的自然语言示例，补上结尾“图”。
- `metadata.yaml` 的依赖声明与 `requirements.txt` 对齐。

## v2.5.0 (2026-05-27)

### 新增
- 新增 AstrBot WebUI 插件页面「图片历史」，可查看已成功发送的 Pixiv 图片记录
- 图片历史支持本地缩略图、网格/列表视图、关键词、来源、R18、会话类型筛选和作品黑名单
- 发送记录覆盖搜索、排行榜、作品下载和签到 Pixiv 背景，只有发送成功后才写入
- 新增 SQLite 当天图片索引，用于普通发图避免重复
- 新增每日签到，支持 HTML 卡片、Pixiv/自定义背景、金币、好感度、连续签到和好感度双倍加持
- 新增管理员命令 `/签到测试`，可预览签到卡片和背景且不写入签到数据

### 改进
- 新增 `Pillow` 依赖用于生成本地缩略图
- 原始发送图片仍按原逻辑清理，仅保留缩略图和元数据
- 图片历史默认保留最近 200 条，淘汰记录时同步清理旧缩略图
- 普通发图去重改为按北京时间当天持久保存，插件重启后仍有效，次日自动清理
- 签到背景沿用 R18 模式、拉黑标签和作品 ID 黑名单过滤
- 签到 Pixiv 背景会写入图片历史并保留本地缩略图

### 修复
- 修复 SQLite 连接未显式关闭时，Windows 下测试或清理临时数据库可能遇到文件占用的问题

### 测试
- 补充 `ImageIndexStore` 的原子占用、释放、次日清理和黑名单回归测试

## v2.3.0 (2026-05-25)

### ✨ 新增
- **自然语言自动触发**（需在配置中开启 `auto_trigger_enabled`）
  - 群内发送「来一份图」「来三张初音ミク图」等自然语言时自动触发 Pixiv 发图
  - 支持中文数字（一到十、两）和阿拉伯数字
  - 支持标签搜索和排行榜（无标签时）
  - 受频率限制保护

### 🔧 改进
- **模块拆分**：将 PixivClient、ImageDownloader、AiCommenter 拆分为独立模块
  - `pixiv_client.py` — Pixiv API 客户端
  - `downloader.py` — 图片下载、URL 提取、临时文件管理
  - `ai_commenter.py` — AI 识图评论管道
- 减少代码重复，提升可维护性

### 🐛 修复
- 修复自动触发未生效：`super().__init__` 需传入 `config` 参数
- 自动触发命中后消费事件，避免同一消息继续被其他处理器重复处理
- 正则改为必须以「图」结尾，避免「来份外卖」等误触发

### 📝 配置
- 新增 `auto_trigger_enabled` 配置项（自然语言自动触发开关）

## v2.2.0 (2026-05-24)

### ✨ 新增
- **发送重试机制**：图片发送失败时自动重试，最多3次
  - 递增等待时间（2秒、4秒）
  - 适用于所有发送场景：搜索结果、排行榜、作品下载
  - 详细的重试日志记录
- **合并转发失败降级**：合并转发失败后自动降级为逐条发送
- **失败提示内嵌合并消息**：下载失败的作品ID显示在合并消息末尾
- **日志友善提示**：发送失败时日志显示友善提示+原始错误，用户端保持原样

## v2.1.0 (2026-05-24)

### ✨ 新增
- **作品ID下载**：`/pd <作品ID> [页码]` 通过作品ID直接下载并发送图片
  - 支持多页作品指定页码：`/pd 12345678 2`（下载第2页）
  - 自动 R18 内容过滤检查
  - 原图过大时自动降级到低质量图片
  - 支持 AI 识图评论（如果启用）

### 🔧 改进
- 帮助文档新增 `/pd` 命令说明
- 版本号更新至 v2.1.0

## v2.0.0 (2026-05-23)

### ✨ 新增
- **PixivClient 类**：封装 API 认证、缓存、自动刷新逻辑
- **排行榜支持**：8 种排行类型，`/prl` 查看详情
- **作品详情**：`/pi <ID>` 查看标题/作者/标签/浏览量/收藏数
- **帮助指令**：`/ph` 显示使用说明
- **短命令入口**：当前使用 `/p`、`/pr`、`/prl`、`/pi`、`/pd`、`/ph`
- **请求频率限制**：防刷屏，可配置间隔秒数
- **多页作品支持**：正确获取多页作品图片
- **平台限定**：仅支持 aiocqhttp（OneBot）平台

### 🔧 改进
- **重构为 PixivClient 类**：API 逻辑与插件逻辑分离
- **全局 aiohttp 会话**：异步安全，正确的生命周期管理
- **完善的错误处理**：分类错误提示（超时/HTTP错误/网络异常）
- **详细的日志输出**：统一 `[GetPx]` 前缀
- **完善的 terminate 清理**：正确关闭 API 和 HTTP 会话
- **发送前提示**：每张图片附带标题和作品ID
- **频率限制反馈**：告知用户需等待秒数

### 📝 配置
- 新增 `rate_limit_seconds` 配置项（请求频率限制）
- 更新 `pixiv_ranking_mode` 提示信息
