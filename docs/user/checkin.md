# 签到说明

返回 [README](../../README.md) · [指令参考](commands.md)。

## 发奖与群榜

签到奖励按用户全局统计，同一用户每天只能成功领取一次。群排行则按用户实际在哪个群发送过签到分别记录；同一天可以进入多个群榜，但不会重复获得金币。

| 行为 | 规则 |
| --- | --- |
| 首次签到 | 发放金币和好感度，生成签到卡片 |
| 连续签到 | 连续天数增加，奖励随连续天数提升 |
| 漏签 | 连续天数重置为 1，累计签到天数不清零 |
| 重复签到 | 不重复发奖、不扣好感度，重新发送当天已保存的同一张卡片 |
| 签到测试 | 使用当前用户资料、生日、称号、事件和问候配置预览卡片；在线背景在 5 分钟内避免重复，但不发奖、不增加签到次数，也不写入正式去重索引或群排行 |
| 主题预览 | `/查看主题 <编号>` 直接发送内置预览图，不校验是否购买，不调用在线图片源/T2I，不修改任何签到数据 |

## 金币商店

| 商品 | 价格 |
| --- | --- |
| 好感度双倍加持 1 天 | 200 金币 |
| 好感度双倍加持 3 天 | 500 金币 |
| 好感度双倍加持 7 天 | 1000 金币 |
| 更新当天在线背景 | 100 金币，可配置 |
| 非默认签到主题 | 每套 1500 金币，可配置 |

加持购买后立即记录有效期。如果当天已经签到，则从明天签到开始影响收益；重复购买会延长有效期。

默认“米白”永久免费。购买其他主题后会自动切换，已购买主题可以免费反复切换；当天已经签到时，重新发送 `/签到` 即可按新主题重建卡片。背景更新仅适用于 `pixiv_daily` 模式，只有成功找到并保存新作品后才扣金币。

## 好感度等级

| 好感度 | 等级 |
| --- | --- |
| `< 0` | 排斥 |
| `0 - 9.99` | 陌生 |
| `10 - 29.99` | 熟悉 |
| `30 - 69.99` | 亲近 |
| `70 - 139.99` | 信赖 |
| `140+` | 挚友 |

## 卡片与背景

签到卡片固定渲染为 `960 × 540` 的 H 纸张画册：左侧展示身份、问候、今日奖励和成长进度，右侧使用固定 `3:4` 相框。背景作品按 `3:4 ± 20%` 严格筛选，只接受宽高比 `0.60–0.90`；签到背景固定下载 `medium` 画质，不受普通发图画质配置影响，也不会先下载原图再降级。Lolicon 为首选图片源；失败时有标签回退 Pixiv 搜索，无标签回退 Pixiv 推荐作品。作品下载失败、损坏或所有候选都无匹配时，相框显示内置纸张占位图，图片始终使用 `object-fit: contain` 完整展示，不裁切原图。

首次签到生成的问候、作品信息和最终 JPEG 会保存供当天复用。重复签到不再扣除好感度，也不会重新发奖、重新选图或再次调用问候模型，而是优先重发同一张缓存卡片；JPEG 缓存仅保留一天，缺失或损坏时根据当天记录重建。

## 问候

签到问候可选择本地事件文案、[一言 API](https://developer.hitokoto.cn/sentence/) 或 AstrBot 文本模型，默认使用一言。AI 模式可单独选择文本模型；未指定时尝试当前会话文本模型。AI 问候默认最多 32 个中文字符、最多两句话；超长或内容不合规时回退本地文案。一言类型可在配置页使用中文多选，单次签到仍只会在所选类型范围内随机返回一句；选择“全部”或不选择时从全部分类随机。一言正文限制为最多 24 个字符，每位用户当日首次签到仅请求一次，句子和卡片会随当日签到记录复用；使用 `/刷新签到背景` 时，一言模式会重新请求一句并更新当天卡片，接口失败时保留原问候；API 超时、报错、返回空文本、超长或内容不合规时，首次签到回退到对应事件的本地文案，不影响奖励和卡片发送。

## 生日、事件与称号

生日只保存月日，不保存出生年份。首次签到会在 OneBot QQ 平台尝试一次资料读取；直接使用 `/签到生日` 可查看生日，并在尚未保存时重新读取 QQ 资料。QQ 资料未公开时会给出提示，用户也可以手动设置或清除生日。管理员可以添加插件全局年度事件和单次事件。主事件按“生日、单次事件、年度事件、节假日、签到里程碑、连续签到”排序，其他同日事件进入次要备注。

累计签到和连续签到会解锁称号：初见旅人、七日同行、月下常客、百日珍藏、周年相守和千日物语。老用户在下一次签到、查看成就或查看称号时会按当前数据补发已满足的称号；尚未佩戴称号时自动佩戴当前已解锁的最高称号，之后可用 `/佩戴称号` 自行切换。

## 节假日数据

法定节假日与调休数据来自 `holiday-cn` 年度数据，并保存在 AstrBot 插件数据目录。插件首次安装、插件版本变化时会在后台尝试更新一次；正常运行期间距上次成功更新满 `180` 天后再次更新。网络失败不会阻塞插件启动或签到，会继续使用旧缓存，并由内置公历日期和 `lunar-python` 农历节日规则兜底。每次更新会预取当前年份和下一年份的数据。

## 数据与清理（签到相关）

- 签到在线背景参考当天 SQLite 索引；正式签到会预占用索引，失败时释放占用，候选全用过时继续请求下一批。
- 签到数据保存在 AstrBot 插件数据目录中。
- 签到卡片 JPEG 缓存由签到缓存模块按天自行过期，重启时不会被整目录清空，当天重复签到仍可复用。插件日常启动不会删除数据库、黑名单和备份；仅在管理员通过管理中心导入 JSON 备份时覆盖签到数据（导入前会自动创建回滚备份）。
